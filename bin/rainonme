#!/bin/bash
echo "---Running Selenium tests"
export STAGING_SERVER='http://muffinbandits.herokuapp.com/'
mvn test-compile failsafe:integration-test failsafe:verify
if [ $? -eq 0 ]
  then
    bin/success
  else
          bin/failure
fi
